extern int printf(const char *, ...);

void main() {
	
	printf("%d\n", 3 & 6);

	/**
	 * In, ans = x & y 	if (x <= y)
	 * might hold true for ans <= x
	 * But I will stick to binary operation rather than any trick or observation
	 */ 
}	
