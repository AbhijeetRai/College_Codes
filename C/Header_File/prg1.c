#include "abhijeet.h"	//header file inside " " tells, compiler to look for that header file
#include<stdio.h>	// in the current directory, where code is present

void main() {

	printf("%.2lf\n", square(5));
}
