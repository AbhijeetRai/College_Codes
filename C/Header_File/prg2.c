#include "stdio.h"
#include "fact.h"

/**
 * I have written a header file fact.h in /usr/include (for that root access is needed) so we can use <> or ""
 * to import that header file.
 * fact.h has a function to find factorial of a number.
 */

void main() {

	fact(5);
}
