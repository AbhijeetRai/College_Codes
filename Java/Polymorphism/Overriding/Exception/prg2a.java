import java.io.*;

class FrenchRevolution {

        void name() throws IOException {

        }
}
/**
 * It is not necessary that, if parent is throwing something. We must throw it, too.
 * Similarly if parent method is throwing an exception, it is not necessary that child method must throw it too.
 */
class HaitianRevolution extends FrenchRevolution {

        void name() {

        }
}
