class FrenchRevolution {

        void name() throws ArithmeticException {

        }
}
/**
 * If parent throws something, it is not necessary that we must throw it, too.
 * Similarly, if parent method throws an exception, it is not necessary that child method must throw it too.
 */
class HaitianRevolution extends FrenchRevolution {

        void name() {
                
        }
}
