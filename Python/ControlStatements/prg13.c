#include<stdio.h>

void main() {

        for (int i = 0; i < 2; i++) {

                int a = 0;
        }
        printf("%d\n",a);
}

/**
 * Scope is a limited only in for loop, because it is declared there, not out of it
 */
