#include<stdio.h>

void main() {

        int x = 10;

        if(x > 50)
                printf("x if gretaer than 50");
                printf("x = %d",x);

        else
                printf("x is smaller than 50");
}
